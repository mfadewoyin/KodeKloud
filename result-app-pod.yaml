apiVersion: v1
kind: Pod
metadata:
  name: result-app-pod
  labels:
    name: myapp-result
    app: demo-myapp
spec:
  containers:
    - name: result-app
      image: kodekloud/examplevotingapp_result:v1
      ports:
        - containerPort: 80